apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name     : myapp2-helm
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name     : in-cluster
    namespace: monitoring
  source:
    path   : "HelmCharts/Chart2"
    repoURL: "git@github.com:nazarito0/ArgoCD.git"
    targetRevision: HEAD
  project: default
  syncPolicy:
    automated:
      prune   : true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true  