apiVersion: v1
kind: Secret
metadata:
  name: db-secret
type: Opaque
stringData:
# Кожне значення перетворено в base64 (наприклад, через команду echo -n "..." | base64)
  DB_HOST: "postgres-service"
  DB_USER: "postgres"
  DB_PASS: "password"
  DB_NAME: "postgres"