apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
# Кожне значення перетворено в base64 (наприклад, через команду echo -n "..." | base64)
  POSTGRES_USER: "postgres"
  POSTGRES_PASSWORD: "password"
  POSTGRES_DB: "postgres"